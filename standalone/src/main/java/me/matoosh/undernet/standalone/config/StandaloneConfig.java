package me.matoosh.undernet.standalone.config;

/**
 * The config for the standalone application.
 */
public interface StandaloneConfig {
    /**
     * The currently selected network identity.
     * @return
     */
    String identity();
}
